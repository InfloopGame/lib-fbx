# Project Brief

`@infloopgame/lib-fbx` 是 InfloopGame 的 TypeScript npm 库，目标是解析与处理 FBX（ASCII / Binary）。

当前范围：可发布的库脚手架、格式探测、`parse()` / `buildScene()`、完整单测与覆盖率门槛、GitHub Actions CI、基于 OIDC 的 npm 发布。动画求值尚未实现。
